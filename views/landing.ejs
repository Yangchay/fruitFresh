<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Fruitify - Fresh Fruits Delivered</title>
  <style>
    :root {
      --main-color: #ff6347;
      --accent-color: #4caf50;
      --light-bg: #fdfdfd;
    }


    body {
      margin: 0;
      font-family: 'Segoe UI', sans-serif;
      background-color: var(--light-bg);
      color: #333;
    }


    header {
      background-color: white;
      padding: 1rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }


    .logo {
      font-size: 1.5rem;
      font-weight: bold;
      color: var(--accent-color);
    }


    nav a {
      margin-left: 1.5rem;
      text-decoration: none;
      color: var(--main-color);
      font-weight: 500;
    }


    .hero {
      display: flex;
      flex-direction: column;
      align-items: center;
      text-align: center;
      padding: 3rem 2rem;
      background: linear-gradient(to bottom right, #ffe5b4, #fff5e1);
    }


    .hero h1 {
      font-size: 3rem;
      margin-bottom: 1rem;
      color: var(--accent-color);
    }


    .hero p {
      font-size: 1.2rem;
      max-width: 600px;
      margin-bottom: 2rem;
    }


    .hero img {
      max-width: 90%;
      width: 400px;
      border-radius: 1rem;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
    }


    .btn-group {
      margin-top: 2rem;
    }


    .btn {
      background-color: var(--main-color);
      color: white;
      padding: 0.8rem 1.5rem;
      margin: 0 0.5rem;
      border: none;
      border-radius: 5px;
      text-decoration: none;
      font-size: 1rem;
      transition: background 0.3s;
    }


    .btn:hover {
      background-color: #e5533d;
    }


    @media (max-width: 600px) {
      .hero h1 {
        font-size: 2rem;
      }


      .btn-group {
        display: flex;
        flex-direction: column;
        gap: 1rem;
      }


      .btn {
        margin: 0 auto;
        width: 100%;
        max-width: 300px;
      }
    }
  </style>
</head>
<body>
  <header>
    <div class="logo">Fruitify 🍎</div>
    <nav>
      <a href="/login">Login</a>
      <a href="/signup">Signup</a>
    </nav>
  </header>


  <section class="hero">
    <h1>Fresh Fruits Delivered to You</h1>
    <p>Welcome to Fruitify – your one-stop solution for the freshest fruits delivered right to your doorstep. Eat healthy, live healthy!</p>
    <img src="" alt="Fresh fruits"/>
    <div class="btn-group">
      <a class="btn" href="/signup">Get Started</a>
      <a class="btn" href="/login">Already Have an Account?</a>
    </div>
  </section>
</body>
</html>
